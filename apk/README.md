# APK Repository

Alpine `apk` packages. Only `paseo*` / `tone3000*`.

Add to `/etc/apk/repositories`:

```
https://xpufx.github.io/xpufx-pkgs/apk/x86_64
```

Then `apk update && apk add paseo-cli-git`.
