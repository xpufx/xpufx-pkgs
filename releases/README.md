# Releases

Standalone tarballs and checksums.

Note: current binaries (pacman packages, AppImages, standalone tarballs)
ship as assets on the rolling GitHub Release
(`arch-x86_64-current`), not as git-tracked files — several exceed
GitHub's 100MB git-file limit.
